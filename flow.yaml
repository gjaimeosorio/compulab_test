diagram:
  url: https://raw.githubusercontent.com/gjaimeosorio/compulab_test/refs/heads/main/flow.svg

cells:

  inbox_depth:
    dataRef: A.start_rate.values[0]
    label:
      textPattern: 'Inbox: ${value}'

  start_rate:
    dataRef: A.start_rate.values[0]
    label:
      textPattern: 'Rate: ${value}'

